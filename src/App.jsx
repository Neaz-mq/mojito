import gsap from 'gsap';
import { ScrollTrigger, SplitText } from "gsap/all";

gsap.registerPlugin(ScrollTrigger, SplitText);

const App = () => {
 return (
	<main>
	 <div>
        <h1 className='text-7xl text-amber-500'>sfsfsf</h1>
     </div>
	</main>
 )
}

export default App